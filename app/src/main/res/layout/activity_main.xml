<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="9dp"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/tv_form_pendaf"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="FORMULIR PENDAFTARAN"
        android:textStyle="bold"
        android:gravity="center_horizontal"
        android:layout_centerHorizontal="true"/>

    <EditText
        android:id="@+id/et_namle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nama Lengkap"
        android:layout_marginTop="12dp"
        android:layout_below="@id/tv_form_pendaf"/>

    <EditText
        android:id="@+id/et_no_daftar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nomor Pendaftaran"
        android:layout_marginTop="12dp"
        android:layout_below="@id/et_namle"/>

    <Spinner
        android:id="@+id/sp_jalur"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/et_no_daftar"
        android:layout_marginTop="12dp"
        android:entries="@array/jalur_daftar"
        />

    <CheckBox
        android:id="@+id/cb_kon"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Konfirmasi Daftar"
        android:layout_marginTop="12dp"
        android:layout_below="@id/sp_jalur"/>

    <Button
        android:id="@+id/btn_reg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/cb_kon"
        android:layout_marginTop="12dp"
        android:text="Daftar"/>

</RelativeLayout>